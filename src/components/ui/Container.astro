---
type ContainerSize = "default" | "narrow" | "wide";

interface Props {
  size?: ContainerSize;
  class?: string;
  id?: string;
}

const {
  size = "default",
  class: extraClass = "",
  id = "",
} = Astro.props as Props;

const sizes: Record<ContainerSize, string> = {
  default: "max-w-7xl",
  narrow: "max-w-5xl",
  wide: "max-w-[90rem]",
};
---

<div id={`${id}`} class={`${sizes[size]} mx-auto px-6 ${extraClass}`}>
  <slot />
</div>
