---
import Section from "../../ui/Section.astro";
import Container from "../../ui/Container.astro";
import Badge from "../../ui/Badge.astro";
import Heading from "../../ui/Heading.astro";
import Text from "../../ui/Text.astro";

import TelegramButton from "../../ui/TelegramButton.astro";
import { useTranslations, getLanguageFromPath } from "../../../i18n/utils";

const lang = getLanguageFromPath(Astro.url.pathname);
const t = useTranslations(lang);
---
<Section class="py-32">
  <Container class="grid lg:grid-cols-2 gap-20 items-center">
    <div class="relative h-[380px] rounded-2xl overflow-hidden bg-white/10 reveal from-left">
    
       <div class="absolute inset-0 bg-[radial-gradient(circle_at_top,rgba(255,215,120,0.25),transparent_60%)]">
      
      <img 
        src="/social_images/service_image_1.webp" 
        alt="Trading Bot Interface"
        class="
          absolute inset-0 w-full h-full object-cover 
          opacity-50 mix-blend-overlay
          transition-transform duration-700 ease-out 
          group-hover:scale-110
        "
      />

      <div class="absolute inset-0 bg-[linear-gradient(135deg,rgba(255,215,120,0.25),transparent_60%)] pointer-events-none"></div>
      
      <div class="absolute inset-0 grid-bg opacity-30 pointer-events-none"></div>

    </div>
      <div class="absolute bottom-6 left-6 right-6 text-sm text-white/80">
        {t("hero.liveMarketStructure")}
      </div>
    </div>
    <div class="reveal from-right">
      <Badge class="mb-4">{t("signals.badge")}</Badge>
      <Heading level={2} class="mb-6">{t("signals.title")}</Heading>
      <Text muted class="mb-6">{t("signals.description")}</Text>
      <ul class="mb-8 space-y-2 text-sm text-white/70">
        <li>{t("signals.entryStopTargets")}</li>
        <li>{t("signals.capitalProtection")}</li>
        <li>{t("signals.noOvertrading")}</li>
      </ul>
     <TelegramButton
      type="channel"
      username="eldoradofxfree"
    >
      {t("signals.accessFreeSignals")}
    </TelegramButton>
    </div>

     
  </Container>
</Section>